namespace GD
{
    /// <summary>
    /// Used to denote state of doors, windows, portals
    /// </summary>
    /// <see cref="DoorController"/>
    public enum DoorStateType
    {
        Open,
        Opening,
        Closing,
        Closed
    }
}