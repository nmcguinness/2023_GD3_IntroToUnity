namespace GD
{
    public class UIManager : Singleton<AudioManager>
    {
        // Start is called before the first frame update
        private void Start()
        {
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}